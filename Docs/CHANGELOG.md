# CHANGELOG

## 2026-08-05 — Version 0.9.1

调整：

- 收获飞行特效改成区分图标：飞向金币的是💰，飞向经验的是⭐（之前两颗都用⭐，分不清哪个代表什么）
- 仓库满了点收获，不再是静默失败：弹窗提示"仓库满了，是否升级？"，点"去升级"直接跳转仓库场景，点"取消"关掉弹窗、地里的作物保留
- 新增通用确认弹窗 `Client/src/ui/ConfirmDialog.ts`（消息 + 确认/取消两个按钮），以后类似"是否xxx"的地方可以直接复用

## 2026-08-05 — Version 0.9.0

新增：

- 收获成功瞬间的反馈：两颗⭐从地块飞向金币/经验位置（`Client/src/ui/Particles.ts`），同时尝试播放音效（`Client/src/core/SoundManager.ts`）
- 渲染循环从每秒刷新一次改成每帧刷新（`requestAnimationFrame`），飞行动画才能连贯，倒计时也更新得更及时
- 新建 `Client/assets/audio/` 作为音频素材的实际存放位置（部署包内，不是仓库根目录的 `Assets/`），目前没有真实音效文件，代码已接好、文件缺失时安静地不报错

修复文档：

- [11_技术架构.md](11_技术架构.md) / [10_美术规范.md](10_美术规范.md) 更正素材存放位置说明——`Assets/` 在部署包外面，运行时读不到，真正能用的素材要放 `Client/assets/`

## 2026-08-04 — Version 0.8.2

新增：

- 点"全部卖出"改成先弹窗显示这次卖了多少钱（"卖出成功 +X金币"），点确定才关掉，不再是直接默默加到金币里
- 新增通用结果提示弹窗 `Client/src/ui/ResultDialog.ts`，以后类似"一次性反馈"的地方可以直接复用

## 2026-08-04 — Version 0.8.1

新增：

- 等级给了实际作用：作物按等级解锁（胡萝卜/玉米 Lv.1、番茄 Lv.3、土豆 Lv.5、西瓜 Lv.10，`config/plants.json` 的 `requiredLevel`），种植菜单里等级不够的作物显示 🔒 和"Lv.X解锁"，点不了
- 玩家等级达到 Lv.15 时仓库容量额外 +50（跟仓库自己升级是两回事，两者叠加），`config/warehouse.json` 的 `capacityBonusPlayerLevel`/`capacityBonus`

## 2026-08-04 — Version 0.8.0

新增：

- 等级系统：经验按曲线换算成等级（`Client/config/levels.json`，baseExp 50 / growthRate 1.3），HUD 显示"Lv.X（经验: Y）"
- 等级目前只是经验的展示换算，没有升级奖励/解锁内容（记在 [04_经济系统.md](04_经济系统.md) 待补充里）

## 2026-08-04 — Version 0.7.2

新增：

- 每种作物的发芽🌱/半成熟🌿图标都不一样了（`config/plants.json` 新增 `sproutIcon`/`growingIcon` 字段），不再是所有作物共用一套通用图标
- 点一下生长中（未成熟）的地块，能看到种的作物名字（再点一下换回倒计时）

## 2026-08-04 — Version 0.7.1

新增：

- 地块之间不再留背景色缝隙，10 块地贴在一起
- 新增场景装饰（`Client/src/ui/Scenery.ts`）：草地背景、天空带+云朵、地块四周的木栅栏
- 好友农场占位场景同步用了同样的背景/栅栏，风格一致

## 2026-08-04 — Version 0.7.0

新增：

- 地块背景改成带纹理的土壤贴图（犁沟线+土块斑点，`Client/src/ui/LandTile.ts`），不再是纯色方块
- 作物三阶段生长视觉：发芽🌱（进度<50%）→ 半成熟🌿（50%~100%，还看不出具体作物）→ 成熟后显示作物自己的图标+金色边框高亮
- 好友农场占位场景同步使用新土壤贴图和生长图标，视觉风格保持一致

## 2026-08-04 — Version 0.6.2

调整（UI，反馈调整上一版）：

- 按钮/标题/状态提示改回中文文字（拜访好友、仓库、地图、返回、取消、签到、金币、经验等），只有作物保留 emoji 图标
- 地块背景色不再随成熟状态变色（之前橙色背景跟胡萝卜的橙色图标撞在一起不好看），统一用土色背景，成熟改用金色边框高亮表示

## 2026-08-04 — Version 0.6.1

调整（UI）：

- 全部按钮/状态文字尽量换成图标（emoji），减少界面文字：金币💰、经验⭐、拜访好友👥、仓库📦、地图🗺️、返回🔙、取消✖️、签到🎁、可收获✅等
- 作物新增 `icon` 字段（`Client/config/plants.json`），地块、种植菜单、仓库库存列表都用图标代替作物名文字
- 真实数据类文字（好友名字等）保留，图标只替换纯粹的按钮/标签文字

## 2026-08-04 — Version 0.6.0

新增：

- 每日订单：仓库场景新增"今日订单"区块，从固定订单池（`Client/config/orders.json`）按日期挑一个，同一天不变；交订单扣仓库库存，换金币+经验，一天限交一次
- 订单奖励按需求作物 sellPrice/exp 总和 × 1.5 计算，比直接"全部卖出"划算

## 2026-08-04 — Version 0.5.2

修复：

- 存档读取改成跟默认值浅合并，不再假设旧存档已经有全部字段。旧存档缺 `inventory`/`warehouseLevel` 导致 `this.inventory` 是 `undefined`，一进仓库场景或点收获就在 `Object.values(undefined)` 上崩溃（仓库白屏、收获没反应）

## 2026-08-04 — Version 0.5.1

新增：

- 每次收获额外给固定5金币（不管什么作物），见 `Client/config/harvest.json`；作物本身的钱仍然要卖仓库库存才有
- 金币来源明确为：签到 + 收获固定金币 + 卖仓库作物 + 以后的订单系统（[08_任务系统.md](08_任务系统.md)，还没做）

## 2026-08-04 — Version 0.5.0

新增：

- 仓库系统：1~20级，每级容量+10（20级=200），花费=当前等级×100金币（`Client/config/warehouse.json`）
- 首页新增"仓库"入口按钮，新场景可以看库存、升级仓库、一键"全部卖出"、返回首页

调整（行为变更）：

- 收获不再直接换金币：作物先进仓库占容量，经验依然立刻到手；仓库满了收获会失败，作物留在地里不会丢
- 卖钱统一改成在仓库里点"全部卖出"（真正的商店——分作物卖、可能有折扣等——还没做，见 [06_建筑系统.md](06_建筑系统.md)）

## 2026-08-04 — Version 0.4.1

新增：

- 补齐五种作物：新增土豆（180/260，8分钟，80经验）、西瓜（300/450，15分钟，150经验），凑齐 [12_开发计划.md](12_开发计划.md) 第二周要求的五种植物
- 纯配置改动，种植菜单/地块渲染代码不用改，自动读取全部作物

## 2026-08-04 — Version 0.4.0

新增：

- 每日签到：每天首次进入首页弹窗领取金币（默认10金币，见 `Client/config/checkin.json`），未签到状态和日期存进本地存档
- 抽出通用配置读取工具 `ConfigLoader.ts`，`PlantConfig.ts` 和新的 `CheckInConfig.ts` 共用

## 2026-08-04 — Version 0.3.0

新增：

- 场景切换架构（`Scene` 接口 + `game.ts` 里的场景管理），首页新增"拜访好友"按钮
- 地图场景：两个入口卡片——我的农场 / 好友农场
- 好友农场占位场景：假数据展示，能返回地图，暂不可交互（点赞/浇水/送礼等真实好友互动等服务器和好友数据就绪后再做，见 [07_好友系统.md](07_好友系统.md)）

## 2026-08-04 — Version 0.2.1

新增：

- 种植菜单每种作物显示成熟时间

调整：

- 作物成熟时间改为胡萝卜1分钟/玉米2分钟/番茄5分钟（方便测试）
- 经验值改为按成熟时间正比计算：每1分钟成熟时间=10经验（胡萝卜10/玉米20/番茄50）

## 2026-08-04 — Version 0.2.0

新增：

- 种植菜单支持多种作物选择（胡萝卜/玉米/番茄，数值见 `Client/config/plants.json`，对应 [04_经济系统.md](04_经济系统.md)），金币不够的作物在菜单里禁用
- 地块生长/成熟状态显示具体作物名称

修复：

- HUD 安全区域改用 `wx.getSystemInfoSync().statusBarHeight`，替换掉不稳定的 `getMenuButtonBoundingClientRect`

## 2026-08-04 — Version 0.1.1

修复：

- 作物配置改为通过 `wx.getFileSystemManager().readFileSync` 读取 `config/plants.json`，因为微信小游戏的 `require` 不支持直接加载 `.json` 文件
- HUD 顶部资源栏改用 `wx.getMenuButtonBoundingClientRect()` 计算安全区域，避免被刘海屏/状态栏遮挡

## 2026-08-03 — Version 0.1.0

新增：

- 微信小游戏客户端工程搭建（TypeScript + 原生框架，`Client/project.config.json` / `game.json`）
- 游戏首页：10 块土地网格 + 顶部金币/经验栏
- 点击空地弹出种植菜单，可种植胡萝卜（购买价 20，售价 30，成熟 5 分钟，经验 5，见 `Client/config/plants.json`）
- 成熟后点击收获，获得金币与经验
- 本地存档（`wx.setStorageSync`/`getStorageSync`）

## 2026-08-03 — Version 0.0.1

新增：

- 项目文档结构初始化（Docs / Assets / Client / Server）
