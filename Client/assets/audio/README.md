# 音效文件放这里

代码已经接好了播放逻辑（`Client/src/core/SoundManager.ts`），文件不存在时会安静地失败，不影响游戏运行；把对应文件放进来就会自动生效，不用改代码。

| 文件名 | 什么时候响 | 格式 |
| --- | --- | --- |
| `harvest.mp3` | 收获成功的瞬间 | mp3（微信小游戏也支持 aac/wav，改文件名对应改 `SoundManager.ts` 里的 `src` 路径即可） |

时长建议 0.3~1 秒，不用太长，收获很频繁。
